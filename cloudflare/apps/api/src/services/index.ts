/**
 * Business logic services
 * Reusable service layer for complex operations
 */

// Placeholder for service modules
// Services would handle data transformations, calculations, and business logic

export const services = {
  // Invoice service
  invoice: null,
  // Expense service
  expense: null,
  // Forecast service
  forecast: null,
  // Tax service
  tax: null,
};
